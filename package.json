cat <<EOF > package.json
{
  "name": "konashi",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "build": "electron-builder --win --portable"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.0.0"
  }
}
EOF
